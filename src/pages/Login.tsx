import { AuthFormContainer } from "@/components/auth/AuthFormContainer";

export default function Login() {
  return (
    <AuthFormContainer>
      <div>Login Form</div>
    </AuthFormContainer>
  );
}